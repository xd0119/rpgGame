# 暴雨孤岛·旧案重演 —— 作品集项目介绍（STAR）

## Situation · 背景
一个集剧情、推理、美术、状态管理于一体的单文件 Web 项目。

## Task · 任务
在有限时间内独立完成：① 修复全部已知交互缺陷；② 重做美术资产解决"图文不一致"和"白底立绘"；③ 让推理线可被持续加厚（提供可改剧本）；④ 产物需可直接部署、零后端、纯前端运行。

## Action · 行动
- 真实痛点：AI 生成的角色立绘前后不一致、线索图里的中文文字与剧本对不上（"报纸说了 xxx，图里却是乱码"）、NPC 立绘是白底而非透明 PNG。同时游戏本体也有一批待修的交互问题：推论面板反复提示操作、时间线小游戏直接把答案时间写在卡上、剧情脉络图回跳后分支丢失、乙女结局台词尴尬。
- **交互层修复**：删除推论面板/对话里所有"直接告诉答案"的重复提示，改为失败 ≥6 次才针对性给提示；时间线卡片默认隐藏时间节点、新增"查看提示"按钮按需恢复；移除乙女结局"画中彩虹"及全部乙女文案，定位改为纯悬疑推理；重写剧情脉络图 `drawBranchTree`——诊断出"branchLog 只记录有 choices 的节点，而 resultNode 常是无 choices 的中间节点，导致递归中断"，新增 `findNextBranchNode` 沿 `SCRIPT.next` 链跨越中间节点，并新增 `fitBranchView` 按节点包围盒自动设置 SVG viewBox，实现"进入即定位放大"。
- **美术资产重做**：诊断"AI 生图无法可靠渲染中文"是图文不一致的根因，对 4 条文档类线索（委托函/日记/威胁信/遗嘱）改为 **CSS 渲染纸质文档**，文字与剧本逐字一致；对 3 个 NPC 白底立绘写 `remove_bg.py`（四角采样背景色 + 色彩距离软抠 + 边框连通性 BFS 防误删人物内部 + alpha 羽化），产出透明 PNG，并用透明度统计校验质量（透明 59–81%、内部空洞 <2%）；另生成 6 张内容贴合剧本的对象线索候选图供审阅。
- **工程化**：剧本数据与渲染分离（game-script.js / index.html），localStorage 存档+分支快照+结局图鉴，16:9 自适应画布，BGM 随场景切换；提供 `剧本.md` 作为可改的纯文本剧本并标注推理线加厚方向。
- **质量门**：node --check 通过 game-script.js 语法校验；改动同步至部署目录并保持资产结构一致。

## Result · 成果
- 交付一个**纯前端、零依赖、可直接 GitHub Pages 部署**的互动推理影游，含 10 线索 / 5 推论 / 时间线还原 / 3 结局 / 全屏剧情脉络图（支持回跳与未访问分支可视化）。
- "图文不一致"从根因上解决：文档线索文字 100% 与剧本一致；NPC 立绘全部透明 PNG。
- 剧情脉络图从"只显示第一个节点"修复为完整分支树 + 自适应视野。
- 产物：`index.html`（92KB，单文件含全部样式与逻辑）+ `game-script.js`（可改剧本）+ `剧本.md`（可改叙事稿）+ `remove_bg.py`（可复用抠除工具）。

## 技术栈
HTML/CSS/原生 JS（无框架）、SVG 分支树、localStorage 状态机、PIL+NumPy 图像处理、AI 文生图（角色/场景/线索）。

## 可继续推进
推理线加厚（不在场证明破绽、林作者被诬陷线、手法链补全）、角色立绘跨场景一致性（同一人物多表情）、BGM 动态混音、移动端触控适配。
