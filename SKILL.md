---
name: "rpg-game-workflow"
description: "生成单文件 HTML 推理/RPG 网页游戏的标准工作流：需求追问→框架搭建→剧本填充→美术资源→集成验证→迭代整改。当用户要生成 RPG/推理/视觉小说类网页游戏，或要求按固定流程制作带搜证/推论/盘问/多结局玩法的游戏时调用。"
---

# RPG 游戏生成工作流

本技能沉淀了单文件 HTML 推理/RPG 游戏的标准生成流程。包含：动工前必问清单、固定框架规范、迭代整改检查表。任何 RPG/推理类网页游戏都按此流程推进。

***

## Phase 0：需求追问（动工前必做）

**关键原则：动工前必须向玩家确认以下维度，缺一项都可能返工。** 用 `AskUserQuestion` 一次性批量追问，不要挤牙膏式逐个问。

### A. 题材与基调

1. 题材类型（推理/恋爱/冒险/恐怖/校园…）
2. 故事基调（轻松治愈/悬疑紧张/悲剧/喜剧）
3. 单线还是多结局？几个结局？有没有兜底 Bad End？

### B. 美术与立绘

1. 美术风格（清新漫画/写实/像素/水墨）——**明确拒绝真人风格**
2. 是否接受 AI 生成立绘？能否保证角色一致性？（不能保证→用单张立绘，避免多表情翻车）
3. 文档类线索（信件/日记/遗嘱）用 **CSS 渲染文本** 还是 AI 图片？——默认 CSS（AI 中文文字不可靠）
4. 关键线索图风格要求

### C. 玩法与系统

1. 核心玩法循环（搜证→推论→盘问→指认？还是别的）
2. 数值系统：要几项？叫什么名字？**是否影响结局分支？**（建议不影响，避免好感度绑架结局）
3. 推论失败如何处理？（失败 N 次后浮现「查看提示」按钮+弹窗 / 放弃推理惩罚 / 兜底结局）
4. 搜证后是否自动推进剧情？——**默认玩家点击「▶ 继续剧情」按钮才推进，禁止自动跳转**
5. 是否需要「二次搜证」扩展（ext 节点，搜证后再开放新热点）？
6. 盘问后是否需要追问模块？——默认每个嫌疑人有 2-3 个未问问题可追问
7. 推论完成度是否影响后续节点入口？——默认时间线入口需全部手动推论完成

### D. UI 与布局

1. 画幅比例（默认 16:9）——**所有 UI 元素必须限定在画布内**
2. 立绘显示方式（说话时居中去背景 / 永久显示左侧）——默认前者
3. 数值面板位置（默认左上角紧凑浮动胶囊）
4. 返回按钮统一文案（默认「回到首页」）
5. 场景名用中文还是 ID？（默认中文，禁 node1/node2）

### E. 内容规范

1. 关键提示（证据不足/推论失败/线索不足）是否 sticky（玩家点击关闭）？——**默认 sticky，避免玩家没看完就消失**
2. 盘问响应是否打字机效果+点击推进？——默认是
3. 选项是否只保留剧情推进类，删除「查看页面」类选项？——默认是
4. 剧情脉络是否默认全显示节点（未探索显示「??? 未走过的分支」灰虚线）？——默认是
5. 指认嫌疑人是否需要特定推论完成才能进行？——默认是

***

## Phase 1：框架搭建（固定骨架，不可省）

### 文件结构

- `index.html`：核心 UI + 交互逻辑（单文件，内联 CSS/JS）
- `game-script.js`：剧本数据独立文件（CHARACTERS/CLUES/DEDUCTIONS/ENDINGS/SCRIPT/SCENE\_NAMES/HOTSPOTS/TIMELINE）
- `assets/`：立绘 `characters/`、背景 `bg/`、线索 `clues/`、bgm `bgm/`

### 固定架构

1. **16:9 game-canvas 容器**：所有 UI 用 `absolute` 挂载其内，禁止 `fixed` 相对网页定位
2. **封面菜单**：新游戏 / 剧情脉络 / 能力图鉴（顺序固定，不设全屏封面/结局图鉴）
3. **节点系统**：`SCRIPT` 对象（每个节点含 `bg/lines/choices/requireDed/hotspots/next` 等）+ `SCENE_NAMES` 中文映射
4. **搜证**：`HOTSPOTS` 热点 + 搜证后 `showSearchContinueBar()` 显示「▶ 继续剧情」按钮
5. **推论**：5 槽位点击填入 + 失败计数 + 失败6次浮现「💡 查看提示」按钮弹窗 + 放弃推理扣值
6. **数值**：6 项能力（推理 lu/共情 shen/洞察 ded/观察 obs/直觉 int/勇气 cou）+ 六边形雷达图 + **statLog 全量记录**（所有加/减分同步写 `state.statLog[key].push({reason, amount, time})`，用于能力图鉴点击查明细）
7. **剧情脉络**：SVG 全节点预渲染 + `localStorage` 探索计数（`markExplored/isExplored`）+ 未探索「???」占位；**过渡节点不要放进 mainTrunk**（只放主干剧情节点）；多结局节点选项**水平展开**（不同 x 坐标），结局链垂直向下不重叠
8. **存档**：localStorage 持久化 + 节点快照（`branchSnapshots`）支持回跳；**结局节点必须删存档**（`removeItem(SAVE_KEY)` + 隐藏"继续游戏"按钮），`returnToCover()` 检查结局节点时不重写存档
9. **追问模块**：`needQLogNot` 字段过滤已问问题，复用原选项 eff/台词，追问子节点不进 mainTrunk
10. **条件过滤**：`needAllDeds`/`hideIfAllDeds` 配合使用实现"推论未完成时只显示推论入口，全完成时只显示时间线"

***

## Phase 2：剧本填充

### 必产出物

- `剧本.md` 原稿先行（角色表/线索表/推论表/结局表/节点表）
- **时间线表格**（避免顺序矛盾，每个事件标精确时间）
- 搜证节点与盘问问题的**线索引用匹配检查表**

### 关键约束

- 搜证节点与盘问问题引用的线索必须匹配（盘问问题只能引用当前节点可获取的线索，不能引用后续节点线索）
- 推论 `need` 中的线索必须在对应搜证节点可获取
- 嫌疑人在场必须有逻辑理由（纪念聚会/产权问题/职务关系）
- 证据持久性必须解释（老宅封存/暴雨冲刷发现/夹层底稿）
- 指认嫌疑人需特定推论完成（如指认 A 需先破不在场证明推论）
- **推论门槛统一用 `.every`**（不要 `.some`），并在旁白里准确描述门槛数量
- **推论奖励必须分散**：不能全给一个属性。手动推论 ≈ 7 条，每条给 1-2 属性，覆盖至少 4 种属性
- **选项 eff 数值要去重统计**：同一节点的互斥选项取最优路径，算每个属性的理论最大值，确保区间收窄在 ±5 以内
- **推论 reward 字段名必须和 stat 字段名一致**（lu/shen/ded/obs/int/cou），新增属性时三个处理函数（doDeduce/checkAutoDeductions/系统协助）都要同步补

***

## Phase 3：美术资源

- 立绘：**PNG 透明背景**，禁 JPG 带背景
- 背景图：清新漫画风（日式动画风、干净墨线、暖色），拒真人
- 文档类线索：**CSS 渲染文本**，图片仅作缩略图/插图
- 立绘一致性：AI 无法保证多表情一致→用单张立绘，手动修图比 AI 重生成稳定
- 关键线索图：清新漫画风，避免真人/写实
- 图片生成 prompt 含 safety 词时用替代词（如「砒霜纸包」→「油纸药包」绕过审核）

***

## Phase 4：集成与验证

1. `game-script.js` 同步剧本（CHARACTERS/CLUES/DEDUCTIONS/ENDINGS/SCENE\_NAMES/SCRIPT/HOTSPOTS/TIMELINE）
2. `index.html` 同步 UI（SCENE\_ORDER 等数组）
3. 语法检查：`node --check game-script.js` + 提取内联 JS 做 `new Function()` 语法校验
4. **交叉引用校验**：线索数/推论数/结局数/节点数/场景名数 全部匹配，0 错误
5. **statLog 完整性校验**：遍历所有加分点，确认每个都写了 statLog（搜证+1、推论+1、推论 reward、时间线奖励、放弃推理扣分）
6. **数值平衡校验**：跑最优路径算每个属性理论最大值，区间应收窄在 ±5 以内
7. **结局存档校验**：触发结局后检查 localStorage 已清除 + "继续游戏"按钮已隐藏
8. 同步到目标目录（用户指定路径）

***

## Phase 5：迭代整改检查表（每次用户反馈对照）

用户反馈后，按以下维度逐项对照检查，避免遗漏：

| 维度    | 检查项                               |
| ----- | --------------------------------- |
| 画布约束  | 所有 UI 是否在 16:9 画布内？窗口是否 80% 宽不全屏？ |
| 立绘规范  | 说话时居中去背景？群像先隐藏单人层？PNG 透明底？        |
| 文档线索  | CSS 渲染还是 AI 图片？图片仅作缩略？            |
| 关键线索图 | 清新漫画风？拒真人？                        |
| 场景名   | 中文（花园/书房）？禁 node1？                |
| 数值    | 洞察/共情命名？是否影响结局？statLog 全量记录？？是否平衡（区间±5）？ |
| 指认嫌疑人 | 需特定推论完成才能进行？                      |
| 提示    | sticky toast？玩家点击关闭？              |
| 盘问    | 对话框+打字机？点击推进？追问模块？           |
| 选项    | 只留剧情推进类？删查看页面？                    |
| 搜证后   | 显示按钮？禁止自动跳转？                      |
| 剧情脉络  | 默认全节点？未探索显示「???」？主干无过渡节点？多结局选项水平展开？无重叠？ |
| 嫌疑人在场 | 逻辑合理？                             |
| 证据持久  | 有解释？                              |
| 推论失败  | 6次后查看提示弹窗？放弃扣值+兜底结局？              |
| 结局存档  | 触发结局后存档已清？"继续游戏"已隐藏？returnToCover 不重写结局存档？ |
| 时间线入口 | 推论未完成时只显示推论入口？全完成时只显示时间线？     |
| 布局  | 六边形雷达图是否固定高度？称号列表是否可滚动？不被挤没？ |

***

## 关键技术避坑（Lessons）

- 动态 UI 用**事件委托**（`dataset.clueId`），不要 `onclick` 直接绑定（动态重渲染会丢事件）
- Toast 关键提示必须 sticky，普通提示自动消失会丢信息
- AI 生成中文文档图片不可靠→CSS 渲染
- 多表情立绘 AI 难一致→单张+手动修图
- 立绘去背景用硬阈值（RGB≥248）+连通保护，软阈值会丢五官
- 群像显示必须显式隐藏单人层，否则会和主角重叠
- 搜证后禁止自动跳转 choice 页，必须给「▶ 继续剧情」按钮
- 推论系统：失败6次后弹「查看提示」按钮（不要把提示常驻底部），放弃推理扣值+3次触发兜底结局
- `Date.now()` 别少括号；拖拽合成逻辑复杂易错→能用自动解锁就别手写合成
- koa-connect 包装会 ctx 泄漏→原生 Koa 重写
- **statLog 是正式功能**：不是调试日志，所有数值变动（含搜证+1、推论硬编码+1、时间线、放弃推理扣分）必须同步写
- **结局存档三重防线**：showEnding 删存档 + 隐藏按钮、returnToCover 不重写结局存档、结局→能力图鉴关闭自动回首页
- **剧情脉络 mainTrunk 只放主干**：追问子节点/过渡节点不要放，否则横向排列导致总宽爆炸 + 汇聚线交叉重叠
- **多结局节点选项必须水平展开**：垂直堆叠会导致结局链水平重叠；子节点选项 y 必须在父节点下方，不能和父节点同 y
- **推论 reward 分散化**：全给一个属性会导致数值悬殊（如旧版 ded31 vs shen8），推论本身是"理解角色动机"的载体，给对应的情感属性更合理
- **六边形雷达图 CSS**：不要用 `height:auto;aspect-ratio:1/1`（flex 布局会被压到 0），用 `flex:0 0 固定值; height:固定值`；称号列表用 `flex:1 1 auto; overflow-y:auto` 可滚动

***

## 触发本技能后 Agent 的行为

1. 检测到「生成 RPG/推理/视觉小说游戏」意图时自动加载本流程
2. **第一步永远是 Phase 0 追问**——用 `AskUserQuestion` 批量确认 A~E 五维度，不要直接动工
3. 追问收敛后，按 Phase 1→5 顺序推进，每阶段完成即同步+验证
4. 用户反馈时，对照 Phase 5 检查表逐项整改
5. 全程遵守 Hard Constraints（画布内 UI、中文场景名、sticky 提示等）
